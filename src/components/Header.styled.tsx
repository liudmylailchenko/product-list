import { AppBar, Toolbar } from '@material-ui/core';
import styled from 'styled-components/macro';

export const ToolContainer = styled(Toolbar)`
  display: flex;
  justify-content: space-between;
`;

export const HeaderContainer = styled(AppBar)`
  margin-bottom: 30px;
`;
